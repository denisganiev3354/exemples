int distance =10000;
int firstFriendSpeed = 1;
int dogSpeed = 5;
int Time = 0;
if ( distance > 10 )
{
    Console.WriteLine ("Время пройденое собакой");
    Console.Write ( Time = distance / ( firstFriendSpeed + dogSpeed ));
    Console.Write (" минут");
}